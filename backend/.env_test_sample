# Use :memory: for zero-setup testing (SQLite in-memory). No PostgreSQL required.
# Or set a PostgreSQL DSN to test against a real DB (e.g. CI).
DATABASE_DSN=":memory:"
# DATABASE_DSN="host=localhost user=test password=test dbname=testdb port=5432 sslmode=disable TimeZone=UTC"
JWT_SECRET="test-secret"